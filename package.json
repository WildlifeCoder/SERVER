{
  "name": "sass",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^4.1.1",
    "node-sass": "^7.0.0"
  },
  "scripts": {
    "serve": "live-server",
    "sass-watch": "node-sass --watch sass/styles.scss assets/css/styles.css",
    "dev": "concurrently --kill-others \"npm run serve\" \"npm run sass-watch\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
